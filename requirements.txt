# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Offline AI Video Generator
#
# Install with:
#   pip install -r requirements.txt
#
# NOTE: PyTorch is NOT listed here because the correct wheel depends on your
# hardware.  Install it first via setup.sh (recommended) or manually:
#
#   CUDA 12.1+  →  pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cu121
#   ROCm 6.0    →  pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/rocm6.0
#   CPU only    →  pip install torch==2.3.1 torchvision==0.18.1 --index-url https://download.pytorch.org/whl/cpu
#
# Dependency compatibility notes:
#   - diffusers >= 0.32.0  requires huggingface-hub >= 1.0
#   - transformers < 4.47  requires huggingface-hub < 1.0  ← CONFLICT
#   - transformers >= 4.47 dropped the <1.0 upper bound   ← RESOLVED
#   - tokenizers >= 0.21.0 is required by transformers >= 4.47
# ─────────────────────────────────────────────────────────────────────────────

# ── Diffusion / model stack ──────────────────────────────────────────────────
diffusers>=0.32.0
# transformers >= 4.47 dropped huggingface-hub<1.0 cap — required for diffusers 0.32+
transformers>=4.47.0
accelerate>=0.34.2
safetensors>=0.4.4
sentencepiece>=0.2.0
tokenizers>=0.21.0

# ── HuggingFace Hub ───────────────────────────────────────────────────────────
huggingface_hub>=1.0.0

# ── Version checking ─────────────────────────────────────────────────────────
packaging>=24.0

# ── Video / image I/O ────────────────────────────────────────────────────────
imageio>=2.35.1
imageio-ffmpeg>=0.5.1      # bundles static ffmpeg binary — no system ffmpeg needed
Pillow>=10.4.0
numpy>=1.26.4
opencv-python-headless>=4.10.0.84

# ── Optional quality-of-life ─────────────────────────────────────────────────
psutil>=6.0.0
tqdm>=4.66.5

# ── Memory-efficient attention (strongly recommended for T4 / <24 GB GPUs) ───
# xformers replaces O(N²) attention with a tiled kernel — prevents CogVideoX
# attention OOM on cards with < 24 GB VRAM.
# Install the build matching your torch+CUDA version:
#   pip install xformers==0.0.27 --index-url https://download.pytorch.org/whl/cu121
xformers>=0.0.27
